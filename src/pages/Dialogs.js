import Row from '../components/Containers/Row';
import Button from '../components/Buttons/Button';

const Dialogs = () => (
  <Row className='margin-bottom'>
    <Button type='button' text='Open dialog' color='blue-filled' />
    <code>deneme</code>
  </Row>
);

export default Dialogs;
